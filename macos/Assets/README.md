# TopicTidy application icon

`AppIcon.png` was generated using the built-in imagegen tool (not a local API script). The build derives all ICNS sizes with sips/iconutil.

Prompt: Use case: logo-brand. Asset type: macOS application icon for TopicTidy, a private local file organizer that groups documents into topics. Generate a single polished 1024x1024 icon, centered macOS rounded-square tile with ample transparent exterior margins, genuine transparent background. An elegant blue translucent inbox tray containing three neatly aligned cream white document cards with small teal and amber topic tabs. Subtle dimensional Apple-native material, soft studio light, restrained depth, crisp simple silhouette readable at 32px. No letters, no text, no sparkles, no watermark, no surrounding scene. Production application icon, not a mockup.
